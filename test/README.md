# Hardening tests

These tests exercise cf2past inside Cloudflare's Workers Vitest runtime. During the hardening branch, regressions are added before the production fix so CI records the expected RED state before implementation.
