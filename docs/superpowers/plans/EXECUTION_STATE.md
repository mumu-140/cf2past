# Comprehensive hardening execution state

- Design: approved.
- Plan: approved for execution.
- Current phase: RED regression harness.
- Production implementation has not started in this execution phase.
- CI temporarily uses `npm install` only to regenerate the dependency lock during bootstrap; final CI must return to `npm ci` before merge.
